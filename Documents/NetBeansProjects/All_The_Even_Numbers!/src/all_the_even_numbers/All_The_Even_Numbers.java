/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package all_the_even_numbers;

/**
 *
 * @author raynehebert
 */
public class All_The_Even_Numbers {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) { 
        int evens = 0;
        for(int i = 0; i < 100;i++){
            if(i % 2 == 0);
                evens = evens + 1;
        }
        System.out.println("There are "+evens+" evens in");
    }    
    static boolean isPrime( int number){
        int divisor = 0;
        for (int i = 2; i < number; i++){
            if (number % 1 == 0){
                divisor += 1;
            
            }
        }
        
            
    }
        static boolean isEven( int number){
        return (number % 2 == 0);
    }

    static boolean isOdd( int number){
        return (number % 2 == 0);
    }   
            
    }
/*  
What is a method?
    A method is a function
    It is like a program a little program
    that communicates with other parts of
    the program by returningvalues (most of the time).
    Like finding a square root, something you 
    might want to do again and again
*/

    
    
    
        //A return value is the type of/*
        //value you are giving back to the program
            
    
        // TODO code application logic here
    
    
